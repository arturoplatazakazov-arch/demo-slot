# Gold of Baku 2 — ассеты (3x3)

Синий рескин `gold-of-baku`: механика, символьные коды и вёрстка те же, меняется
только арт. Слаг игры — `gold-of-baku-2`. Пути отсюда зашиты в
`front/js/gold-of-baku-2/slot.js` (`ASSET_ROOT`), при ЛЮБОЙ замене файла
поднимать там `ASSET_VERSION` — статика раздаётся без Cache-Control, и браузер
покажет старую картинку. То же и с `?v=` в `front/gold-of-baku-2.html`
(фон, рама и логотип подставлены прямо в разметке).

## Синий арт (заменён 20.08.2026)

Синими приехали фон (ландшафт + портрет), рама, логотип, `seven`, `bar` и
`tea`. Из золотой версии остались как есть `symbols/orange.png`,
`symbols/grape.png`, `symbols/pomegranate.png`, `symbols/bell.png` — синих
вариантов этих четырёх не было. `img/cover.jpg` пересобрана из нового арта
(скрипт-композиция: фон → сукно → рама → 3x3 символов → логотип).

Сукно барабана и палитра в `front/css/gold-of-baku-2.css` перекрашены из бордо
в тёмную синь (`--carpet/--carpet-deep/--ink`, градиент `.reel`), иначе проём
читался красной дырой в синей раме.

**Доли проёма пересняты с новой рамы** заливкой изнутри (не сканом строк: скан
протекает через зазор между нижним брусом и ковровой полкой и выдаёт окно до
низа картинки). `cabinet.png` = 1565x864, пропорция 1.811, окно 175..1391 по X
и 73..749 по Y, то есть `--win-l 0.1118 / --win-t 0.0845 / --win-w 0.7776 /
--win-h 0.7836`. От пропорции 1.811 считается и `--cab-h` в обоих режимах
(десктоп 1300→718, портрет 950→524). Перерисуешь раму — пересними доли и
пересчитай `--cab-h`, на глаз не подгоняй.

**Сверяй bbox альфы присланной рамки с версией «на чёрном».** У золотой версии
вырезатель фона съел нижнюю ковровую панель (альфа обрывалась на y=836); у этой
рамы панель на месте, но проверять надо каждый раз.

Все PNG квантованы на месте (256 цветов, FASTOCTREE). Не-квантованные исходники
как пришли — в `src/*_blue_raw.png` (папка в `.gitignore`).

## Попапы выигрыша

`img/popup_big.png`, `popup_mega.png`, `popup_epic.png` — присланные плашки
(1448x1086, титул нарисован на самой картинке). Игра рисует поверх только
сумму, и ложится она в ПУСТУЮ рамку внизу плашки: доли рамки сняты с каждого
PNG заливкой по цвету и лежат в `.game-popup--big/--mega/--epic` в CSS
(`--amt-l/-t/-w/-h`). Заменишь плашку — пересними доли, у трёх штук они разные.

Два подводных камня разметки (оба уже учтены в CSS, не сломай при правках):
попап висит в `.screen`, а НЕ в масштабируемом `#stage`, поэтому канвасные px
домножаются на `--stage-scale` руками; и центрируется он по центру РАМЫ
(`--cab-t + --cab-h/2`), а не экрана — иначе в портрете плашка встаёт под
слотом, над пустым фоном.

Исходники как пришли — `src/pop_raw_*.webp`; в `img/` лежат квантованные
(256 цветов, 7.0 МБ → 1.3 МБ, на глаз не отличить, в игре плашка ~400px шириной).

## spine/ — анимации символов

`spine/<code>/animation.json` + `animation.atlas` + текстура (имя страницы —
как в первой строке атласа: `orange.png`, `seven.png` и т.д.). Контракт с
движком: `SpineResource.load()` ищет в папке ИМЕННО `animation.json` и
`animation.atlas`, поэтому присланные `granade/pomegranate.json` и
`777/seven.json` переименованы, а папки названы кодами символов.

Экспорт несёт три клипа — `idle`, `win` и (у seven/bar/bell/tea) `land`.
Игра играет ТОЛЬКО `win`: idle крутил бы девять скелетов с бликами в вебвью,
а land дублирует падение ленты, которое уже делает landReel. Включить их —
одна строка в slot.js.

Сверка с PNG-статикой (тело символа без fx-слоёв, попиксельно):

| клип | первый кадр | последний кадр |
|---|---|---|
| `win`  | = статика у всех 7 | = статика у всех 7 |
| `land` | символ на 340-430px выше (падение) | = статика |
| `idle` | сдвиг 0-6px у seven/bar/bell/orange/tea, 0-1px у grape/pomegranate | то же, что и первый (цикл замкнут) |

Поэтому подмена PNG на спайн и обратно на входе/выходе `win` идёт без скачка.
Габариты тела в setup-позе совпадают с размерами `symbols/*.png` до пикселя
(273x195, 308x186, 300x200, 352x206, 363x220, 365x227, 399x173) — это и
проверяет `bodyBounds()` в slot.js, вписывая скелет в ячейку по той же `--sym`,
что и статику. Перевыгрузишь скелет — сверь заново, вписывание держится на этом.

## img/

| файл | что это | размер (золотой версии) |
|---|---|---|
| `bg_base.jpg`     | фон десктоп | 1798x875 |
| `bg_base_mob.jpg` | фон портрет | 931x1690 |
| `cabinet.png`     | рама с орнаментом, проём прозрачный | 1479x870 |
| `logo.png`        | логотип, обрезан по содержимому | 1400x410 |
| `cover.jpg`       | обложка каталога | 900x470 |

## symbols/

Семь символов, обрезаны вплотную по содержимому (вписываются в ячейку
`object-fit: contain`, оптический вес правится через `--sym` в CSS):
`orange`, `grape`, `pomegranate`, `tea`, `bell`, `bar`, `seven`.

Имена файлов — контракт с `_SYMBOLS` в `app/seed/gold_of_baku_2.py` и
`SYMBOL_CODES` в `slot.js`: код символа == имя PNG. Не переименовывать.
`tea` — это стаканы армуды, `pomegranate` — гранат.
